# Optional. ocr_service.py falls back to the default Windows Tesseract path.
TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe

# Used by the unused FastAPI /chat/ route (backend/services/openai_service.py).
# The live chatbot in the frontend uses GROQ_API_KEY instead.
# OPENAI_API_KEY=
